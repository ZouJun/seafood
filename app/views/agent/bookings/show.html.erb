<% content_for :content_for do %>
    <ol class="breadcrumb">
      <li><a href="#">订单管理</a></li>
      <li class="active">分配</li>
    </ol>

    <div class="add_agent">
      <%= form_for @order, url: agent_booking_path(@order), method: :put do |f| %>
          <div class="form-group">
            <%= f.select :dispatcher_id, current_account.dispatchers.all.map{|t| [t.name, t.id]}, {include_blank: '请选择派送人'}, class: "form-control", id: "exampleInputEmail1" %>
          </div>
          <button type="submit" class="btn btn-default">提交</button>
      <% end %>
    </div>
<% end %>